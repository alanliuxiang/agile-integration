--> Deploying template "openshift/amq-broker-72-basic" to project amq

     Red Hat AMQ Broker 7.2 (Ephemeral, no SSL)
     ---------
     Application template for Red Hat AMQ brokers. This template doesn't feature SSL support.

     A new messaging service has been created in your project. It will handle the protocol(s) "openwire,amqp,stomp,mqtt,hornetq". The username/password for accessing the service is usernqA/edhSMXCP.

     * With parameters:
        * Application Name=broker
        * AMQ Protocols=openwire,amqp,stomp,mqtt,hornetq
        * Queues=
        * Addresses=
        * AMQ Username=usernqA # generated
        * AMQ Password=edhSMXCP # generated
        * AMQ Role=admin
        * AMQ Name=broker
        * AMQ Global Max Size=100 gb
        * AMQ Require Login=
        * AMQ_EXTRA_ARGS=
        * AMQ Anycast Prefix=
        * AMQ Multicast Prefix=
        * ImageStream Namespace=openshift
        * Image=amq-broker-72-openshift:1.1

--> Creating resources ...
    service "broker-amq-jolokia" created
    service "broker-amq-amqp" created
    service "broker-amq-mqtt" created
    service "broker-amq-stomp" created
    service "broker-amq-tcp" created
    deploymentconfig "broker-amq" created
    route "console" created
--> Success
    Access your application via route 'console-amq.apps.0d94.openshift.opentlc.com' 
    Run 'oc status' to view your app.
